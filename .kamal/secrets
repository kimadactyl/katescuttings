# Secrets for Kamal deployment
# These are read from environment variables - set them before deploying

# GitHub Container Registry token (create at github.com/settings/tokens with packages:write)
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails master key for credentials
RAILS_MASTER_KEY=$(cat config/master.key)

# PostgreSQL password
POSTGRES_PASSWORD=$POSTGRES_PASSWORD

# Database connection URL (constructed from postgres password)
DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@katescuttings-db:5432/katescuttings_production

# Admin login credentials
USERNAME=$ADMIN_USERNAME
PASSWORD=$ADMIN_PASSWORD
