<article>
  <h1>Blog Admin Index</h1>

  <table>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.created_at %></td>
      </tr>
    <% end %>
  </table>
</article>
